{
  "name": "sparc-bloomerang-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Operator scripts for the SPARC Email → Bloomerang pipeline (snapshot load, dedupe worklist, lookup sync). Run with npx tsx; not part of the Netlify site build.",
  "scripts": {
    "load-snapshot": "tsx load-bloomerang-snapshot.ts",
    "load-snapshot:dry": "tsx load-bloomerang-snapshot.ts --dry-run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "csv-parse": "^5.5.6"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
